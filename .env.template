# 1Password のシークレット参照テンプレート。実際の値はコミットしない。
#   op inject -i .env.template -o .env
# 生成した .env は Bun (bun run / bun test) と Wrangler が自動で読み込む。

# Wrangler が .env を Worker の binding として読み込むのを止める。
# (止めないと以下の値がローカルの Worker に渡り、worker-configuration.d.ts にも出力される)
CLOUDFLARE_LOAD_DEV_VARS_FROM_DOT_ENV=false

# 動作確認・e2e・eval 用。auto-router は BYOK なので Worker 自体はこのキーを持たない。
OPENROUTER_API_KEY="{{ op://onkriwa4bh66f64wdj7nqvsize/ngbcwpqk5rw5b45pe4afacvyi4/credential }}"

# Wrangler CLI 用 (deploy / D1 migration など)。
CLOUDFLARE_ACCOUNT_ID="{{ op://gs7ss3lfsqfzc3utkcu54rzshq/mki45tk23ic5axurviugvby3ze/vqjlacf3pgqzl62nkrkazwd6vy }}"
CLOUDFLARE_API_TOKEN="{{ op://gs7ss3lfsqfzc3utkcu54rzshq/mki45tk23ic5axurviugvby3ze/frp64jjycv6vaa25qdc4kuymry }}"
